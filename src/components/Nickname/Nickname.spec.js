/*import Nickname from './Nickname';
import { shallow } from 'enzyme';

describe ('Nickname tests', () => {
    it('should pass', () => )
    let input = '';
    const component= shallow(<Nickname onInput={(e) => { input = e.target.value; }} />);
    }  
});*/